# ─── Direct dependencies for Universal Privacy Layer backend ───
# Transitive dependencies (eth-utils, starlette, pymongo, etc.)
# are pulled in automatically by pip.

# Web framework & server
fastapi==0.110.1
uvicorn==0.25.0
python-dotenv==1.2.1
python-multipart==0.0.22

# Database (async MongoDB)
# pymongo is PINNED here (not left to pip's transitive resolve) on purpose.
# Leaving it floating made the June-26 build resolve pymongo 4.17.0, which
# removed the private _QUERY_OPTIONS symbol that motor 3.3.1 imported from
# pymongo.cursor — ImportError at startup -> CrashLoopBackOff -> Azure kept
# serving the stale (pricing-still-present) image. motor 3.7.1 is the release
# line built for modern pymongo (>=4.9) and no longer references that symbol.
motor==3.7.1
pymongo==4.17.0

# Validation
pydantic==2.12.5

# Ethereum / Web3
web3==6.15.0
eth-account==0.11.0
eth-keys==0.5.0

# Cryptography
pycryptodome==3.20.0

# HTTP client
httpx==0.28.1

# Testing
pytest==9.0.2
requests==2.32.5
